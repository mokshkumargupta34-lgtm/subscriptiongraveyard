# ---- Database (Supabase Postgres) --------------------------------
# Transaction pooler (port 6543) — the app's runtime connection
DATABASE_URL=postgresql://postgres.nstqhtjbizhwphocqwcw:[PASSWORD]@aws-0-ap-northeast-1.pooler.supabase.com:6543/postgres
# Direct connection (port 5432) — migrations & seeds only
DIRECT_URL=postgresql://postgres.nstqhtjbizhwphocqwcw:[PASSWORD]@aws-0-ap-northeast-1.pooler.supabase.com:5432/postgres

# ---- Auth (Phase 2) -----------------------------------------------
AUTH_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# 32 bytes hex (64 chars): node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
TOKEN_ENC_KEY=

# ---- Billing (Phase 5) --------------------------------------------
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_ID=

# ---- Email alerts (Phase 4) ---------------------------------------
RESEND_API_KEY=

# ---- Cron guard (Phase 3) -----------------------------------------
CRON_SECRET=

# ---- App ----------------------------------------------------------
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ---- Optional LLM receipt parsing (Phase 3) -----------------------
# ANTHROPIC_API_KEY=
# LLM_PARSE=true
